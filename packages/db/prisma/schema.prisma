// CleanerCMS Prisma schema.
// Models will be added per the spec in /CLAUDE.md and /docs/spec.md.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
